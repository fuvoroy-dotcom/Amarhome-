
{
  "entities": {
    "Design": {
      "title": "Design",
      "description": "Architectural design layout including walls, pillars, openings, name, and saved estimations.",
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "Name of the project"
        },
        "objects": {
          "type": "array",
          "description": "List of design objects on the canvas"
        },
        "estimations": {
          "type": "object",
          "description": "Saved calculation inputs for all sections"
        },
        "prices": {
          "type": "object",
          "description": "Saved unit prices and other expenses"
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time"
        }
      }
    }
  },
  "auth": {
    "providers": ["google.com"]
  },
  "firestore": {
    "designs/{designId}": {
      "schema": { "$ref": "#/backend/entities/Design" },
      "description": "Collection of saved architectural designs and estimations"
    }
  }
}
